import { describe, it, expect } from 'vitest';
import recipeDisplay from '@src/components/recipeDisplay';

describe('recipeDisplay.js', () => {
  it('should have a default export defined', () => {
    expect(recipeDisplay).toBeDefined();
  });
});